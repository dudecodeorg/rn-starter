import { createIconSetFromIcoMoon } from '@expo/vector-icons';
import * as selection from '../../../assets/iconmoon/selection.json';

const Icon = createIconSetFromIcoMoon(selection, 'IcoMoon', 'icomoon.ttf');

export default Icon;
